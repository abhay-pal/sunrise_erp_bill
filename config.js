window.APP_CONFIG = {
  // For Netlify + Apps Script API routes via redirects/proxy:
  API_BASE_URL: '/api',
  // If your Google Apps Script endpoint requires action query routing directly,
  // set e.g. "https://script.google.com/macros/s/DEPLOYMENT_ID/exec"
  APPS_SCRIPT_WEBAPP_URL: ''
};
